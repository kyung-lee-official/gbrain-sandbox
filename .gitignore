# bun / turbo
node_modules/
.turbo/
.next/

# Secrets
.env
.env.local

# gbrain local state (if GBRAIN_HOME=./.gbrain)
.gbrain/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
.cursor/

# Nested git for shared-source (gbrain sync requires a git repo at the source path)
shared-source/.git/

# Local test page (copy from README)
shared-source/test-demo.md
