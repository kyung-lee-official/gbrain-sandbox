# bun / turbo
node_modules/
.turbo/
.next/

# Secrets
.env
.env.local

# gbrain local state (if GBRAIN_HOME=./.gbrain)
.gbrain/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
.cursor/

# Prisma Client (generated)
apps/api/src/generated/

# Nested gbrain knowledge repo (own .git). Parent never tracks its contents.
# Clone or `git init` into apps/gbrain — see docs/GBRAIN_SETUP.md.
apps/gbrain/
